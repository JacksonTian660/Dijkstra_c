#include <stdio.h>
#include <stdlib.h>
#include "main.h"
#include "dijkstra.h"


int main(){
    
    

    int start = 25719;
    int end = 3500;
    
    number_of_edge_sets = -1;
    number_of_vertices = 0;

    
    dijkstra(start, end);

    // printf("%d", linked_list_ptr[0].distance);


    // free(linked_list_ptr);
    // free(edge_set_ptr);
   



    return 0;
}

